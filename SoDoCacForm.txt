================================================================================
HỆ THỐNG QUẢN LÝ CÔNG VIỆC
SƠ ĐỒ CÁC FORM VÀ LUỒNG ĐIỀU HƯỚNG
================================================================================

📋 TỔNG QUAN:
Ứng dụng Quản Lý Công Việc được xây dựng bằng C# WinForms với kiến trúc 3 lớp.
Sau đây là sơ đồ chi tiết các form và cách chúng liên kết với nhau.

================================================================================
1. SƠ ĐỒ TỔNG QUAN LUỒNG ĐIỀU HƯỚNG
================================================================================

                    ┌─────────────────────┐
                    │   Program.cs        │
                    │   (Entry Point)     │
                    └──────────┬──────────┘
                               │
                               │ Application.Run()
                               │
                               ▼
                    ┌─────────────────────┐
                    │  frmDangNhap        │          
                    │  (Form Đăng Nhập)   │             
                    │                     │             
                    │  • txtTaiKhoan      │          ◄──┐
                    │  • txtMatKhau       │             │
                    │  • btnXacNhan       │             │
                    │  • linklblDangKy    │             │
                    │  • btnThoat         │             │
                    │  • chkGhiNho        │             │
                    └──────────┬──────────┘             │
                               │                        │
                    ┌──────────┴──────────┐             │
                    │                     │             │
            [Đăng ký]            [Đăng nhập thành công] │
                    │                     │             │
                    ▼                     ▼             │
          ┌─────────────────┐   ┌──────────────────┐    │
          │  frmDangKy      │   │  (Form chính)    │────┘
          │  (Đăng ký)      │   │  [CHƯA CÓ]       │
          │                 │   └──────────────────┘
          │  • txtUsername  │
          │  • txtPassword  │
          │  • txtFullName  │
          │  • txtEmail     │
          │  • btnDangKy    │
          │  • btnThoat     │
          └─────────────────┘

                    ┌──────────────────────┐
                    │  CÁC FORM CHỨC NĂNG  │
                    │  (Được gọi độc lập)  │
                    └──────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ frmThemSuaTask  │  │   frmBaoCao     │  │   frmLichSu     │
│ (Quản lý Task)  │  │   (Báo cáo)     │  │   (Lịch sử)     │
│                 │  │                 │  │                 │
│ • dgvDanhSach   │  │ • lblTongCong   │  │ • dgvLichSu     │
│ • txtTen        │  │ • lblTodo       │  │ • btnTimKiem    │
│ • txtMoTa       │  │ • lblDoing      │  │ • btnChiTiet    │
│ • cmbUuTien     │  │ • lblDone       │  │ • btnThoat      │
│ • cmbTrangThai  │  │ • btnDSQuaHan   │  │                 │
│ • cmbDanhMuc    │  │ • btnDSDGQuaHan │  │                 │
│ • dtpDueDate    │  │ • dgvBaoCao     │  │                 │
│ • btnThem       │  │ • btnThoat      │  │                 │
│ • btnSua        │  │                 │  │                 │
│ • btnXoa        │  │                 │  │                 │
│ • btnTimKiem    │  │                 │  │                 │
│ • btnThoat      │  │                 │  │                 │
└─────────────────┘  └─────────────────┘  └─────────────────┘

                    ┌──────────────────────┐
                    │     frmMain          │
                    │(Form chính - Đề xuất)│
                    │                      │
                    │  • Header Panel      │
                    │  • Dashboard Panel   │
                    │  • Menu Buttons      │
                    │    - Quản lý CV      │
                    │    - Báo cáo         │
                    │    - Lịch sử         │
                    │    - Đăng xuất       │
                    └──────────┬───────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ frmThemSuaTask  │  │   frmBaoCao     │  │   frmLichSu     │
│ (userId)        │  │   (userId)      │  │   (userId)      │
└─────────────────┘  └─────────────────┘  └─────────────────┘

================================================================================
2. CHI TIẾT TỪNG FORM
================================================================================

╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 1: Program.cs (Entry Point)                                          ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/Program.cs
🎯 Chức năng: Điểm khởi đầu của ứng dụng
🔄 Luồng điều hướng:
   1. Application.EnableVisualStyles()
   2. Application.SetCompatibleTextRenderingDefault(false)
   3. Application.Run(new frmDangNhap()) → Mở form đăng nhập

┌─────────────────────────────────────────────────────────────────────────┐
│ static void Main()                                                      │
│ {                                                                       │
│     Application.EnableVisualStyles();                                  │
│     Application.SetCompatibleTextRenderingDefault(false);              │
│     Application.Run(new frmDangNhap());  ← Khởi chạy form đăng nhập   │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 2: frmDangNhap (Form Đăng Nhập)                                     ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmDangNhap.cs
🎯 Chức năng: 
   - Xác thực người dùng (Username + Password)
   - Kiểm tra tài khoản bị khóa (lockout sau 5 lần sai)
   - Lưu thông tin đăng nhập (nếu chọn "Ghi nhớ")
   - Kiểm tra kết nối database

🔑 Các thành phần:
   • txtTaiKhoan: TextBox nhập tên đăng nhập
   • txtMatKhau: TextBox nhập mật khẩu (PasswordChar = '*')
   • btnXacNhan: Button đăng nhập
   • linklblDangKy: LinkLabel mở form đăng ký
   • btnThoat: Button thoát ứng dụng
   • chkGhiNho: CheckBox ghi nhớ thông tin đăng nhập

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Khi khởi động form:                                       │
   │    ├─ TestDatabaseConnection()                               │
   │    └─ LoadRememberedCredentials()                            │
   │                                                              │
   │ 2. Khi click "Đăng ký" (linklblDangKy):                     │
   │    ├─ this.Hide()                                            │
   │    ├─ OpenRegistrationForm()                                 │
   │    │  └─ new frmDangKy().ShowDialog()                        │
   │    ├─ this.Show()                                            │
   │    └─ Nếu đăng ký thành công:                                │
   │       └─ Điền username vào txtTaiKhoan                       │
   │                                                              │
   │ 3. Khi click "Đăng nhập" (btnXacNhan) hoặc nhấn Enter:      │
   │    ├─ PerformLogin()                                         │
   │    │  ├─ Kiểm tra lockout                                   │
   │    │  ├─ Validate input                                      │
   │    │  ├─ AuthenticateUser() → Gọi sp_UserLogin              │
   │    │  └─ ProcessLoginResult()                                │
   │    │                                                         │
   │    └─ Nếu đăng nhập thành công:                              │
   │       ├─ Reset failed attempts                               │
   │       ├─ Lưu thông tin (nếu chọn "Ghi nhớ")                 │
   │       ├─ Hiển thị thông báo chào mừng                        │
   │       ├─ this.DialogResult = DialogResult.OK                 │
   │       └─ this.Close()  ← Đóng form đăng nhập                │
   │                                                              │
   │    ⚠️ LƯU Ý: Sau khi đóng, chưa có form chính nào được mở! │
   │    → CẦN TẠO frmMain HOẶC FORM CHÍNH SAU KHI ĐĂNG NHẬP      │
   │                                                              │
   │ 4. Khi nhấn Enter trong txtTaiKhoan:                         │
   │    └─ Focus sang txtMatKhau                                  │
   │                                                              │
   │ 5. Khi nhấn Enter trong txtMatKhau:                          │
   │    └─ PerformLogin()                                         │
   └──────────────────────────────────────────────────────────────┘

🛡️ Bảo mật:
   • Tối đa 5 lần đăng nhập sai → Khóa tài khoản 15 phút
   • Mật khẩu được hash bằng SHA256 + Salt
   • Kiểm tra tài khoản bị vô hiệu hóa (IsActive = 0)


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 3: frmDangKy (Form Đăng Ký)                                          ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmDangKy.cs
🎯 Chức năng:
   - Đăng ký tài khoản mới
   - Validate thông tin đầu vào
   - Hash mật khẩu trước khi lưu
   - Kiểm tra username đã tồn tại

🔑 Các thành phần:
   • txtUsername: TextBox nhập tên đăng nhập
   • txtPassword: TextBox nhập mật khẩu
   • txtConfirmPassword: TextBox xác nhận mật khẩu
   • txtFullName: TextBox nhập họ tên
   • txtEmail: TextBox nhập email
   • btnDangKy: Button đăng ký
   • btnThoat: Button hủy/đóng form

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Được gọi từ frmDangNhap:                                  │
   │    └─ frmDangKy().ShowDialog()                               │
   │                                                              │
   │ 2. Khi click "Đăng ký" (btnDangKy):                         │
   │    ├─ ValidateInput()                                        │
   │    │  ├─ Kiểm tra các trường không rỗng                     │
   │    │  ├─ Kiểm tra mật khẩu khớp nhau                        │
   │    │  ├─ Kiểm tra email hợp lệ                              │
   │    │  └─ Kiểm tra độ dài mật khẩu (>= 6)                    │
   │    │                                                         │
   │    ├─ HashPassword() → PasswordHelper.HashPassword()        │
   │    │                                                         │
   │    ├─ RegisterUser() → Gọi sp_UserRegister                  │
   │    │                                                         │
   │    └─ Nếu thành công:                                        │
   │       ├─ RegisteredUsername = username                       │
   │       ├─ this.DialogResult = DialogResult.OK                 │
   │       └─ this.Close()                                        │
   │                                                              │
   │ 3. Khi click "Thoát" (btnThoat):                            │
   │    └─ this.Close()                                           │
   └──────────────────────────────────────────────────────────────┘

🛡️ Validation:
   • Username: Không rỗng, không chứa ký tự đặc biệt
   • Password: Tối thiểu 6 ký tự, phải khớp với xác nhận
   • Email: Định dạng email hợp lệ
   • FullName: Không rỗng


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 4: frmThemSuaTask (Form Quản Lý Công Việc)                           ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmThemSuaTask.cs
🎯 Chức năng:
   - Xem danh sách công việc
   - Thêm công việc mới
   - Sửa công việc
   - Xóa công việc (Soft Delete)
   - Tìm kiếm công việc

🔑 Các thành phần:
   • dgvDanhSach: DataGridView hiển thị danh sách công việc
   • txtTen: TextBox tên công việc
   • txtMoTa: TextBox mô tả công việc
   • cmbUuTien: ComboBox mức độ ưu tiên (Cao, Trung bình, Thấp)
   • cmbTrangThai: ComboBox trạng thái (Chưa bắt đầu, Đang làm, Hoàn thành)
   • cmbDanhMuc: ComboBox danh mục (Công việc, Cá nhân, Học tập)
   • dtpDueDate: DateTimePicker hạn chót
   • btnThem: Button thêm mới
   • btnSua: Button sửa
   • btnXoa: Button xóa
   • btnTimKiem: Button tìm kiếm
   • btnThoat: Button đóng form

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Khi khởi động form:                                       │
   │    ├─ LayUserIdHienTai() → Gọi sp_GetFirstUserId             │
   │    └─ HienThiDanhSach() → Gọi sp_GetTasksByFilter            │
   │                                                              │
   │ 2. Khi click "Thêm" (btnThem):                               │
   │    ├─ ValidateInput()                                        │
   │    ├─ ChuyenDoiDoUuTien() → Convert UI → DB                  │
   │    ├─ ChuyenDoiTrangThai() → Convert UI → DB                 │
   │    ├─ Gọi sp_CreateTask                                      │
   │    └─ HienThiDanhSach() → Refresh                            │
   │                                                              │
   │ 3. Khi click "Sửa" (btnSua):                                 │
   │    ├─ Kiểm tra đã chọn task                                  │
   │    ├─ Load dữ liệu vào form                                  │
   │    ├─ ValidateInput()                                        │
   │    ├─ ChuyenDoiDoUuTien()                                    │
   │    ├─ ChuyenDoiTrangThai()                                   │
   │    ├─ Gọi sp_UpdateTask                                      │
   │    └─ HienThiDanhSach() → Refresh                            │
   │                                                              │
   │ 4. Khi click "Xóa" (btnXoa):                                 │
   │    ├─ Kiểm tra đã chọn task                                  │
   │    ├─ Xác nhận xóa                                           │
   │    ├─ Gọi sp_DeleteTask (Soft Delete)                        │
   │    └─ HienThiDanhSach() → Refresh                            │
   │                                                              │
   │ 5. Khi click "Tìm kiếm" (btnTimKiem):                        │
   │    └─ Gọi sp_SearchTasks                                     │
   │                                                              │
   │ 6. Khi click "Thoát" (btnThoat):                             │
   │    └─ this.Close()                                           │
   └──────────────────────────────────────────────────────────────┘

💾 Database Operations:
   • sp_GetFirstUserId → Lấy UserId hiện tại
   • sp_GetTasksByFilter → Lấy danh sách công việc
   • sp_CreateTask → Tạo công việc mới
   • sp_UpdateTask → Cập nhật công việc
   • sp_DeleteTask → Xóa công việc (Soft Delete)
   • sp_SearchTasks → Tìm kiếm công việc

📊 Data Mapping:
   • Priority: "Cao" ↔ "High", "Trung bình" ↔ "Medium", "Thấp" ↔ "Low"
   • Status: "Chưa bắt đầu" ↔ "Todo", "Đang làm" ↔ "Doing", "Hoàn thành" ↔ "Done"
   • Task ID: Tự động tạo format "CV001", "CV002", ...


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 5: frmBaoCao (Form Báo Cáo)                                           ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmBaoCao.cs
🎯 Chức năng:
   - Hiển thị thống kê tổng quan
   - Xem danh sách công việc quá hạn
   - Xem danh sách người dùng có công việc quá hạn

🔑 Các thành phần:
   • lblTongCong: Label tổng số công việc
   • lblTodo: Label số công việc chưa bắt đầu
   • lblDoing: Label số công việc đang làm
   • lblDone: Label số công việc hoàn thành
   • lblUsersWithTasks: Label số người dùng có công việc
   • lblUsersWithOverdue: Label số người dùng có công việc quá hạn
   • btnDSQuaHan: Button danh sách công việc quá hạn
   • btnDSDGQuaHan: Button danh sách người dùng quá hạn
   • dgvBaoCao: DataGridView hiển thị báo cáo chi tiết
   • btnThoat: Button đóng form

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Khi khởi động form:                                       │
   │    ├─ LayUserIdHienTai() → Gọi sp_GetFirstUserId             │
   │    └─ CapNhatThongKe() → Load thống kê                       │
   │                                                              │
   │ 2. Khi form Load:                                            │
   │    ├─ CapNhatThongKe()                                       │
   │    │  ├─ Gọi sp_GetDashboardStats → Thống kê task            │
   │    │  └─ Gọi sp_GetUserStats → Thống kê user                 │
   │    └─ Hiển thị số liệu lên labels                            │
   │                                                              │
   │ 3. Khi click "Danh sách công việc quá hạn" (btnDSQuaHan):    │
   │    ├─ Gọi sp_GetOverdueTasks                                 │
   │    └─ Hiển thị kết quả trong dgvBaoCao                       │
   │                                                              │
   │ 4. Khi click "Danh sách người quá hạn" (btnDSDGQuaHan):      │
   │    ├─ Gọi sp_GetUsersWithOverdueTasks                        │
   │    └─ Hiển thị kết quả trong dgvBaoCao                       │
   │                                                              │
   │ 5. Khi click "Thoát" (btnThoat):                             │
   │    └─ this.Close()                                           │
   └──────────────────────────────────────────────────────────────┘

💾 Database Operations:
   • sp_GetFirstUserId → Lấy UserId hiện tại
   • sp_GetDashboardStats → Thống kê công việc (Todo, Doing, Done, Tổng)
   • sp_GetUserStats → Thống kê người dùng
   • sp_GetOverdueTasks → Lấy danh sách công việc quá hạn
   • sp_GetUsersWithOverdueTasks → Lấy danh sách người dùng quá hạn


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 6: frmLichSu (Form Lịch Sử)                                           ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmLichSu.cs
🎯 Chức năng:
   - Xem lịch sử thay đổi công việc
   - Tìm kiếm lịch sử theo User hoặc Task
   - Xem chi tiết từng bản ghi lịch sử

🔑 Các thành phần:
   • dgvLichSu: DataGridView hiển thị danh sách lịch sử
   • txtTimKiem: TextBox nhập từ khóa tìm kiếm
   • cmbLoaiTimKiem: ComboBox chọn loại tìm (Theo User, Theo Task)
   • btnTimKiem: Button tìm kiếm
   • btnChiTiet: Button xem chi tiết
   • btnLamMoi: Button làm mới danh sách
   • btnThoat: Button đóng form

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Khi khởi động form:                                       │
   │    └─ HienThiDanhSach() → Gọi sp_GetAllTaskHistory           │
   │                                                              │
   │ 2. Khi click "Tìm kiếm" (btnTimKiem):                        │
   │    ├─ ValidateInput()                                        │
   │    ├─ Xác định SearchType (User hoặc Task)                   │
   │    ├─ Gọi sp_SearchTaskHistory                               │
   │    └─ Hiển thị kết quả trong dgvLichSu                       │
   │                                                              │
   │ 3. Khi click "Chi tiết" (btnChiTiet):                        │
   │    ├─ Kiểm tra đã chọn dòng                                  │
   │    └─ Hiển thị MessageBox với thông tin chi tiết             │
   │                                                              │
   │ 4. Khi click "Làm mới" (btnLamMoi):                          │
   │    └─ HienThiDanhSach() → Reload toàn bộ                     │
   │                                                              │
   │ 5. Khi click "Thoát" (btnThoat):                             │
   │    └─ this.Close()                                           │
   └──────────────────────────────────────────────────────────────┘

💾 Database Operations:
   • sp_GetAllTaskHistory → Lấy toàn bộ lịch sử
   • sp_SearchTaskHistory → Tìm kiếm lịch sử theo User hoặc Task

📊 Data Mapping:
   • Action: "Create" → "Tạo mới", "Update" → "Cập nhật", "Delete" → "Xóa"
   • Priority: "High" → "Cao", "Medium" → "Trung bình", "Low" → "Thấp"


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 7: frmMain (Form Chính - ĐỀ XUẤT THIẾT KẾ)                            ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/frmMain.cs (CẦN TẠO MỚI)
🎯 Chức năng:
   - Form chính sau khi đăng nhập thành công
   - Điều hướng đến các form chức năng
   - Hiển thị thông tin user và dashboard nhanh
   - Quản lý session và đăng xuất

🔑 Các thành phần đề xuất:

┌─────────────────────────────────────────────────────────────────────────┐
│ PANEL HEADER (Phần đầu form)                                            │
├─────────────────────────────────────────────────────────────────────────┤
│ • lblXinChao: Label "Xin chào, [Tên người dùng]"                        │
│ • lblUsername: Label hiển thị username                                  │
│ • lblCurrentDate: Label hiển thị ngày giờ hiện tại                      │
│ • btnDangXuat: Button "Đăng xuất"                                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ PANEL DASHBOARD (Phần thống kê nhanh)                                   │
├─────────────────────────────────────────────────────────────────────────┤
│ GroupBox: "Tổng quan công việc"                                         │
│ • lblTongCongViec: Label "Tổng công việc: [số]"                         │
│ • lblTodo: Label "Chưa bắt đầu: [số]"                                   │
│ • lblDoing: Label "Đang làm: [số]"                                      │
│ • lblDone: Label "Hoàn thành: [số]"                                     │
│ • lblQuaHan: Label "Quá hạn: [số]"                                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ PANEL MENU/NAVIGATION (Phần menu điều hướng)                            │
├─────────────────────────────────────────────────────────────────────────┤
│ • btnQuanLyCongViec: Button "Quản Lý Công Việc"                         │
│   └─→ Mở frmThemSuaTask                                                 │
│                                                                         │
│ • btnBaoCao: Button "Báo Cáo"                                           │
│   └─→ Mở frmBaoCao                                                      │
│                                                                         │
│ • btnLichSu: Button "Lịch Sử"                                           │
│   └─→ Mở frmLichSu                                                      │
│                                                                         │
│ • btnThoat: Button "Thoát" (hoặc menu đăng xuất)                        │
│   └─→ Đóng form và quay về frmDangNhap                                  │
└─────────────────────────────────────────────────────────────────────────┘

🔄 Luồng điều hướng:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Constructor:                                              │
   │  public frmMain(int userId, string username, string fullName)│
   │    ├─ InitializeComponent()                                  │
   │    ├─ Lưu userId, username, fullName                         │
   │    ├─ HienThiThongTinUser()                                  │
   │    ├─ CapNhatDashboard() → Load thống kê nhanh               │
   │    └─ CapNhatThoiGian() → Hiển thị ngày giờ                  │
   │                                                              │
   │ 2. Khi click "Quản Lý Công Việc" (btnQuanLyCongViec):        │
   │    ├─ frmThemSuaTask taskForm = new frmThemSuaTask(userId)   │
   │    └─ taskForm.ShowDialog()                                  │
   │                                                              │
   │ 3. Khi click "Báo Cáo" (btnBaoCao):                          │
   │    ├─ frmBaoCao reportForm = new frmBaoCao(userId)           │
   │    └─ reportForm.ShowDialog()                                │
   │                                                              │
   │ 4. Khi click "Lịch Sử" (btnLichSu):                          │
   │    ├─ frmLichSu historyForm = new frmLichSu()                │
   │    └─ historyForm.ShowDialog()                               │
   │                                                              │
   │ 5. Khi click "Đăng xuất" (btnDangXuat):                      │
   │    ├─ Xác nhận đăng xuất                                     │
   │    ├─ this.Hide()                                            │
   │    ├─ frmDangNhap loginForm = new frmDangNhap()              │
   │    ├─ loginForm.ShowDialog()                                 │
   │    └─ this.Close()                                           │
   │                                                              │
   │ 6. Khi form Load:                                            │
   │    └─ CapNhatDashboard() → Gọi sp_GetDashboardStats          │
   └──────────────────────────────────────────────────────────────┘

💾 Database Operations:
   • sp_GetDashboardStats → Thống kê nhanh cho dashboard

📋 Sơ đồ giao diện đề xuất:

┌──────────────────────────────────────────────────────────────────────┐
│                    frmMain - HỆ THỐNG QUẢN LÝ CÔNG VIỆC              │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │  HEADER                                                     │     │
│  │  Xin chào, [Tên người dùng]        [Username]  [Đăng xuất]  │     │
│  │  Hôm nay: [Ngày/Tháng/Năm - Giờ:Phút]                       │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │  TỔNG QUAN CÔNG VIỆC                                        │     │
│  │  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐                │     │
│  │  │Tổng │  │Todo │  │Doing│  │Done │  │Quá  │                │     │
│  │  │ 15  │  │  5  │  │  7  │  │  3  │  │hạn 2│                │     │
│  │  └─────┘  └─────┘  └─────┘  └─────┘  └─────┘                │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │  CHỨC NĂNG                                                  │     │
│  │  ┌─────────────────────┐  ┌─────────────────────┐           │     │
│  │  │  Quản Lý Công Việc  │  │      Báo Cáo        │           │     │
│  │  │     [Button]        │  │     [Button]        │           │     │
│  │  └─────────────────────┘  └─────────────────────┘           │     │
│  │                                                             │     │
│  │  ┌─────────────────────┐  ┌─────────────────────┐           │     │
│  │  │      Lịch Sử        │  │      Thoát          │           │     │
│  │  │     [Button]        │  │     [Button]        │           │     │
│  │  └─────────────────────┘  └─────────────────────┘           │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘

🔧 Implementation đề xuất:

┌─────────────────────────────────────────────────────────────────────────┐
│ public partial class frmMain : Form                                     │
│ {                                                                       │
│     private int _currentUserId;                                         │
│     private string _username;                                           │
│     private string _fullName;                                           │
│                                                                         │
│     public frmMain(int userId, string username, string fullName)        │
│     {                                                                   │
│         InitializeComponent();                                          │
│         _currentUserId = userId;                                        │
│         _username = username;                                           │
│         _fullName = fullName;                                           │
│         HienThiThongTinUser();                                          │
│         CapNhatDashboard();                                             │
│     }                                                                   │
│                                                                         │
│     private void HienThiThongTinUser()                                  │
│     {                                                                   │
│         lblXinChao.Text = $"Xin chào, {_fullName}";                     │
│         lblUsername.Text = $"Tài khoản: {_username}";                   │
│         lblCurrentDate.Text = DateTime.Now.ToString("dd/MM/yyyy HH:mm");│
│     }                                                                   │
│                                                                         │
│     private void CapNhatDashboard()                                     │
│     {                                                                   │
│         // Gọi sp_GetDashboardStats và hiển thị                         │
│     }                                                                   │
│                                                                         │
│     private void btnQuanLyCongViec_Click(object sender, EventArgs e)    │
│     {                                                                   │
│         frmThemSuaTask taskForm = new frmThemSuaTask(_currentUserId);   │
│         taskForm.ShowDialog();                                          │
│         CapNhatDashboard(); // Refresh sau khi đóng form                │
│     }                                                                   │
│                                                                         │
│     // ... các event handlers khác                                      │
│ }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║ FORM 8: Form1 (Form Cũ - KHÔNG SỬ DỤNG)                                    ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Vị trí: QuanLyCongViec/Form1.cs
🎯 Chức năng: CHƯA CÓ - Form trống, có thể xóa hoặc giữ lại để test

⚠️ GHI CHÚ:
   Form này hiện tại chỉ là form trống. Không được sử dụng trong ứng dụng.
   Nên tạo frmMain mới thay vì sử dụng Form1.

================================================================================
3. LUỒNG ĐIỀU HƯỚNG CHI TIẾT
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ LUỒNG 1: ĐĂNG NHẬP VÀ ĐĂNG KÝ                                           │
└─────────────────────────────────────────────────────────────────────────┘

Program.cs
    │
    ▼
frmDangNhap (Form chính khi khởi động)
    │
    ├─→ [Click "Đăng ký"] ──→ frmDangKy.ShowDialog()
    │                              │
    │                              ├─→ [Đăng ký thành công]
    │                              │      └─→ Return DialogResult.OK
    │                              │
    │                              └─→ [Thoát/Cancel]
    │                                     └─→ Return DialogResult.Cancel
    │
    └─→ [Đăng nhập thành công]
           └─→ Lưu userId, username, fullName
           └─→ this.Hide()
           └─→ frmMain mainForm = new frmMain(userId, username, fullName)
           └─→ mainForm.ShowDialog()
           └─→ this.Close()
           
✅ ĐỀ XUẤT: Sau khi đăng nhập, mở frmMain để điều hướng các chức năng


┌─────────────────────────────────────────────────────────────────────────┐
│ LUỒNG 2: FORM CHÍNH (frmMain)                                           │
└─────────────────────────────────────────────────────────────────────────┘

frmDangNhap (Sau khi đăng nhập thành công)
    │
    ▼
frmMain
    │
    ├─→ Load: Hiển thị thông tin user + Dashboard
    │
    ├─→ [Click "Quản lý công việc"] → frmThemSuaTask.ShowDialog()
    │
    ├─→ [Click "Báo cáo"] → frmBaoCao.ShowDialog()
    │
    ├─→ [Click "Lịch sử"] → frmLichSu.ShowDialog()
    │
    └─→ [Click "Đăng xuất"] → Quay về frmDangNhap


┌─────────────────────────────────────────────────────────────────────────┐
│ LUỒNG 3: QUẢN LÝ CÔNG VIỆC (frmThemSuaTask)                             │
└─────────────────────────────────────────────────────────────────────────┘

frmMain [Click "Quản lý công việc"]
    │
    ▼
frmThemSuaTask
    │
    ├─→ Load: Tự động hiển thị danh sách công việc
    │
    ├─→ [Click "Thêm"] → Validate → Insert → Refresh
    │
    ├─→ [Click "Sửa"] → Load data → Validate → Update → Refresh
    │
    ├─→ [Click "Xóa"] → Confirm → Soft Delete → Refresh
    │
    ├─→ [Click "Tìm kiếm"] → Search → Display results
    │
    └─→ [Click "Thoát"] → this.Close()


┌─────────────────────────────────────────────────────────────────────────┐
│ LUỒNG 4: BÁO CÁO (frmBaoCao)                                            │
└─────────────────────────────────────────────────────────────────────────┘

frmMain [Click "Báo cáo"]
    │
    ▼
frmBaoCao
    │
    ├─→ Load: Tự động load thống kê
    │
    ├─→ [Click "DS công việc quá hạn"] → Hiển thị trong dgvBaoCao
    │
    ├─→ [Click "DS người quá hạn"] → Hiển thị trong dgvBaoCao
    │
    └─→ [Click "Thoát"] → this.Close()


┌─────────────────────────────────────────────────────────────────────────┐
│ LUỒNG 5: LỊCH SỬ (frmLichSu)                                            │
└─────────────────────────────────────────────────────────────────────────┘

frmMain [Click "Lịch sử"]
    │
    ▼
frmLichSu
    │
    ├─→ Load: Tự động hiển thị toàn bộ lịch sử
    │
    ├─→ [Click "Tìm kiếm"] → Search → Display results
    │
    ├─→ [Click "Chi tiết"] → Show MessageBox với thông tin
    │
    ├─→ [Click "Làm mới"] → Reload toàn bộ
    │
    └─→ [Click "Thoát"] → this.Close()

================================================================================
4. VẤN ĐỀ HIỆN TẠI VÀ ĐỀ XUẤT
================================================================================

⚠️ VẤN ĐỀ 1: THIẾU FORM CHÍNH
────────────────────────────────────────────────────────────────────────────
Sau khi đăng nhập thành công, frmDangNhap đóng lại nhưng không có form nào
được mở tiếp. Người dùng không thể truy cập các chức năng.

💡 ĐỀ XUẤT:
   1. Tạo frmMain (Form chính) với menu điều hướng
   2. Sửa Program.cs hoặc frmDangNhap để mở frmMain sau khi đăng nhập thành công
   
   Ví dụ:
   ┌─────────────────────────────────────────────────────────┐
   │ // Trong frmDangNhap.cs, sau khi đăng nhập thành công:  │
   │ this.Hide();                                            │
   │ frmMain mainForm = new frmMain(userId, username);       │
   │ mainForm.ShowDialog();                                  │
   │ this.Close();                                           │
   └─────────────────────────────────────────────────────────┘


⚠️ VẤN ĐỀ 2: CÁC FORM ĐỘC LẬP
────────────────────────────────────────────────────────────────────────────
Các form frmThemSuaTask, frmBaoCao, frmLichSu hoạt động độc lập, không có
cơ chế điều hướng từ form chính.

💡 ĐỀ XUẤT:
   Tạo frmMain với các button/menu để mở các form chức năng:
   
   ┌──────────────────────────────────────┐
   │        frmMain (Form chính)          │
   │                                      │
   │  [Quản lý công việc] → frmThemSuaTask│
   │  [Báo cáo]         → frmBaoCao       │
   │  [Lịch sử]         → frmLichSu       │
   │  [Đăng xuất]       → Quay về frmDangNhap│
   └──────────────────────────────────────┘


⚠️ VẤN ĐỀ 3: THIẾU QUẢN LÝ USER HIỆN TẠI
────────────────────────────────────────────────────────────────────────────
Các form hiện tại đều lấy UserId đầu tiên trong database (sp_GetFirstUserId),
không truyền UserId từ form đăng nhập.

💡 ĐỀ XUẤT:
   1. Lưu UserId sau khi đăng nhập thành công
   2. Truyền UserId qua constructor hoặc property
   3. Sửa các form để nhận UserId thay vì gọi sp_GetFirstUserId

   Ví dụ:
   ┌─────────────────────────────────────────────────────────┐
   │ public class frmThemSuaTask : Form                      │
   │ {                                                        │
   │     private int _currentUserId;                         │
   │                                                          │
   │     public frmThemSuaTask(int userId)                   │
   │     {                                                    │
   │         InitializeComponent();                           │
   │         _currentUserId = userId;                         │
   │         HienThiDanhSach();                              │
   │     }                                                    │
   │ }                                                        │
   └─────────────────────────────────────────────────────────┘


================================================================================
5. SƠ ĐỒ frmMain CHI TIẾT (THIẾT KẾ ĐỀ XUẤT)
================================================================================

╔════════════════════════════════════════════════════════════════════════════╗
║ THIẾT KẾ GIAO DIỆN frmMain                                                 ║
╚════════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────────┐
│                    HỆ THỐNG QUẢN LÝ CÔNG VIỆC                             │
│                           frmMain                                          │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ╔══════════════════════════════════════════════════════════════════╗     │
│  ║  HEADER PANEL                                                    ║     │
│  ╠══════════════════════════════════════════════════════════════════╣     │
│  ║  ┌──────────────────────────────────────────────────────────┐   ║     │
│  ║  │  Xin chào, [Tên người dùng]                              │   ║     │
│  ║  │  Tài khoản: [Username]                                    │   ║     │
│  ║  │  Hôm nay: [dd/MM/yyyy] - [HH:mm]          [Đăng xuất]    │   ║     │
│  ║  └──────────────────────────────────────────────────────────┘   ║     │
│  ╚══════════════════════════════════════════════════════════════════╝     │
│                                                                            │
│  ╔══════════════════════════════════════════════════════════════════╗     │
│  ║  DASHBOARD PANEL - TỔNG QUAN CÔNG VIỆC                          ║     │
│  ╠══════════════════════════════════════════════════════════════════╣     │
│  ║  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        ║     │
│  ║  │ TỔNG     │  │ TODO     │  │ DOING    │  │ DONE     │        ║     │
│  ║  │   15     │  │    5     │  │    7     │  │    3     │        ║     │
│  ║  └──────────┘  └──────────┘  └──────────┘  └──────────┘        ║     │
│  ║                                                                    ║     │
│  ║  ┌──────────┐                                                     ║     │
│  ║  │ QUÁ HẠN │                                                     ║     │
│  ║  │    2     │                                                     ║     │
│  ║  └──────────┘                                                     ║     │
│  ╚══════════════════════════════════════════════════════════════════╝     │
│                                                                            │
│  ╔══════════════════════════════════════════════════════════════════╗     │
│  ║  MENU PANEL - CHỨC NĂNG                                          ║     │
│  ╠══════════════════════════════════════════════════════════════════╣     │
│  ║  ┌──────────────────────────────────────────────────────────┐   ║     │
│  ║  │  ┌──────────────────────┐  ┌──────────────────────┐    │   ║     │
│  ║  │  │  📋 QUẢN LÝ         │  │  📊 BÁO CÁO          │    │   ║     │
│  ║  │  │  CÔNG VIỆC          │  │                      │    │   ║     │
│  ║  │  │  [Button lớn]       │  │  [Button lớn]        │    │   ║     │
│  ║  │  └──────────────────────┘  └──────────────────────┘    │   ║     │
│  ║  │                                                          │   ║     │
│  ║  │  ┌──────────────────────┐  ┌──────────────────────┐    │   ║     │
│  ║  │  │  📜 LỊCH SỬ          │  │  🚪 ĐĂNG XUẤT       │    │   ║     │
│  ║  │  │                      │  │                      │    │   ║     │
│  ║  │  │  [Button lớn]        │  │  [Button lớn]        │    │   ║     │
│  ║  │  └──────────────────────┘  └──────────────────────┘    │   ║     │
│  ║  └──────────────────────────────────────────────────────────┘   ║     │
│  ╚══════════════════════════════════════════════════════════════════╝     │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

╔════════════════════════════════════════════════════════════════════════════╗
║ CẤU TRÚC CODE ĐỀ XUẤT                                                     ║
╚════════════════════════════════════════════════════════════════════════════╝

public partial class frmMain : Form
{
    // Fields
    private int _currentUserId;
    private string _username;
    private string _fullName;
    private Timer _timer; // Để cập nhật thời gian
    
    // Constructor
    public frmMain(int userId, string username, string fullName)
    {
        InitializeComponent();
        _currentUserId = userId;
        _username = username;
        _fullName = fullName;
        
        // Khởi tạo
        HienThiThongTinUser();
        CapNhatDashboard();
        KhoiTaoTimer();
    }
    
    // Hiển thị thông tin user
    private void HienThiThongTinUser()
    {
        lblXinChao.Text = $"Xin chào, {_fullName}";
        lblUsername.Text = $"Tài khoản: {_username}";
        CapNhatThoiGian();
    }
    
    // Cập nhật thời gian
    private void CapNhatThoiGian()
    {
        lblCurrentDate.Text = DateTime.Now.ToString("dd/MM/yyyy - HH:mm:ss");
    }
    
    // Khởi tạo timer để cập nhật thời gian mỗi giây
    private void KhoiTaoTimer()
    {
        _timer = new Timer();
        _timer.Interval = 1000; // 1 giây
        _timer.Tick += (s, e) => CapNhatThoiGian();
        _timer.Start();
    }
    
    // Cập nhật dashboard
    private void CapNhatDashboard()
    {
        try
        {
            DataTable dt = DatabaseHelper.ExecuteStoredProcedure("sp_GetDashboardStats");
            if (dt.Rows.Count > 0)
            {
                DataRow row = dt.Rows[0];
                lblTongCongViec.Text = row["TotalTasks"].ToString();
                lblTodo.Text = row["TodoCount"].ToString();
                lblDoing.Text = row["DoingCount"].ToString();
                lblDone.Text = row["DoneCount"].ToString();
                // Tính quá hạn từ dữ liệu
            }
        }
        catch (Exception ex)
        {
            MessageBox.Show("Lỗi khi tải dashboard: " + ex.Message, "Lỗi", 
                MessageBoxButtons.OK, MessageBoxIcon.Error);
        }
    }
    
    // Event Handlers
    private void btnQuanLyCongViec_Click(object sender, EventArgs e)
    {
        frmThemSuaTask taskForm = new frmThemSuaTask(_currentUserId);
        taskForm.ShowDialog();
        CapNhatDashboard(); // Refresh sau khi đóng
    }
    
    private void btnBaoCao_Click(object sender, EventArgs e)
    {
        frmBaoCao reportForm = new frmBaoCao(_currentUserId);
        reportForm.ShowDialog();
    }
    
    private void btnLichSu_Click(object sender, EventArgs e)
    {
        frmLichSu historyForm = new frmLichSu();
        historyForm.ShowDialog();
    }
    
    private void btnDangXuat_Click(object sender, EventArgs e)
    {
        if (MessageBox.Show("Bạn có chắc muốn đăng xuất?", "Xác nhận", 
            MessageBoxButtons.YesNo, MessageBoxIcon.Question) == DialogResult.Yes)
        {
            _timer?.Stop();
            this.Hide();
            frmDangNhap loginForm = new frmDangNhap();
            loginForm.ShowDialog();
            this.Close();
        }
    }
    
    // Cleanup
    protected override void OnFormClosing(FormClosingEventArgs e)
    {
        _timer?.Stop();
        _timer?.Dispose();
        base.OnFormClosing(e);
    }
}

╔════════════════════════════════════════════════════════════════════════════╗
║ CẬP NHẬT frmDangNhap ĐỂ MỞ frmMain                                        ║
╚════════════════════════════════════════════════════════════════════════════╝

// Trong frmDangNhap.cs, sửa method HandleSuccessfulLogin:

private void HandleSuccessfulLogin(DataRow userRow)
{
    int userId = Convert.ToInt32(userRow["Id"]);
    string username = txtTaiKhoan.Text.Trim();
    string fullName = userRow["FullName"].ToString();
    
    // Reset số lần thất bại
    ResetFailedLoginAttempts();
    
    // Lưu thông tin đăng nhập nếu chọn "Ghi nhớ"
    SaveOrClearRememberedCredentials(username);
    
    // Hiển thị thông báo thành công
    ShowSuccessMessage(fullName);
    
    // Mở form chính
    this.Hide();
    frmMain mainForm = new frmMain(userId, username, fullName);
    mainForm.ShowDialog();
    
    // Đóng form đăng nhập
    this.Close();
}

╔════════════════════════════════════════════════════════════════════════════╗
║ SƠ ĐỒ ĐIỀU HƯỚNG HOÀN CHỈNH (Sau khi triển khai)                          ║
╚════════════════════════════════════════════════════════════════════════════╝

                    ┌─────────────────────┐
                    │   Program.cs        │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │  frmDangNhap        │
                    └──────────┬──────────┘
                               │
                    [Đăng nhập thành công]
                               │
                               ▼
                    ┌─────────────────────┐
                    │    frmMain          │ ◄──┐
                    │  (Form chính)       │    │
                    │                     │    │
                    │  • Header           │    │
                    │  • Dashboard        │    │
                    │  • Menu Buttons     │    │
                    │    - Quản lý CV     │    │
                    │    - Báo cáo        │    │
                    │    - Lịch sử        │    │
                    │    - Đăng xuất      │────┘
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ frmThemSuaTask  │  │   frmBaoCao     │  │   frmLichSu     │
│ (userId)        │  │   (userId)      │  │   (userId)      │
│                 │  │                 │  │                 │
│ ShowDialog()    │  │ ShowDialog()    │  │ ShowDialog()    │
│                 │  │                 │  │                 │
│ → Refresh       │  │ → Không refresh │  │ → Không refresh │
│   Dashboard     │  │                 │  │                 │
└─────────────────┘  └─────────────────┘  └─────────────────┘

================================================================================
6. TÓM TẮT CÁC FORM
================================================================================

┌─────────────────┬──────────────────────────┬─────────────────────────┐
│ Form            │ Chức năng                │ Trạng thái              │
├─────────────────┼──────────────────────────┼─────────────────────────┤
│ Program.cs      │ Entry point              │ ✅ Hoàn thành           │
│ frmDangNhap     │ Đăng nhập                │ ✅ Hoàn thành           │
│                 │                          │ ⚠️ Cần sửa để mở frmMain│
│ frmDangKy       │ Đăng ký                  │ ✅ Hoàn thành           │
│ frmMain         │ Form chính điều hướng    │ ❌ CHƯA CÓ CODE        │
│                 │                          │ 📝 ĐÃ ĐỀ XUẤT THIẾT KẾ │
│                 │                          │   (Xem chi tiết phần 5) │
│ frmThemSuaTask  │ Quản lý công việc        │ ✅ Hoàn thành           │
│                 │                          │ ✅ Đã có constructor    │
│                 │                          │   nhận userId           │
│ frmBaoCao       │ Báo cáo & Thống kê       │ ✅ Hoàn thành           │
│                 │                          │ ✅ Đã có constructor    │
│                 │                          │   nhận userId           │
│ frmLichSu       │ Lịch sử thay đổi         │ ✅ Hoàn thành           │
│ Form1           │ Form cũ (không dùng)     │ ❌ KHÔNG SỬ DỤNG       │
│                 │                          │   Có thể xóa            │
└─────────────────┴──────────────────────────┴─────────────────────────┘

================================================================================
7. HELPER CLASSES VÀ DATA ACCESS
================================================================================

╔════════════════════════════════════════════════════════════════════════════╗
║ DATA ACCESS LAYER                                                          ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 DatabaseHelper.cs
🎯 Chức năng:
   - Lớp trung gian kết nối với SQL Server
   - Thực thi stored procedures
   - Xử lý connection string từ App.config
   - Cung cấp các method:
     • ExecuteStoredProcedure() → Trả về DataTable
     • ExecuteStoredProcedureNonQuery() → Thực thi không trả về dữ liệu
     • TestConnection() → Kiểm tra kết nối database

╔════════════════════════════════════════════════════════════════════════════╗
║ HELPER CLASSES                                                             ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Helpers/PasswordHelper.cs
🎯 Chức năng:
   - Hash mật khẩu bằng SHA256 + Salt
   - Method: HashPassword(string password) → string
   - Được sử dụng trong frmDangNhap và frmDangKy

📄 Helpers/CurrentUser.cs
🎯 Chức năng:
   - Lưu trữ thông tin user hiện tại đang đăng nhập
   - Static class để truy cập từ mọi form
   - Methods:
     • SetCurrentUser(userId, username, fullName)
     • GetUserId() → int
     • GetUsername() → string
     • GetFullName() → string
     • IsLoggedIn() → bool
     • Clear() → Xóa thông tin khi đăng xuất

📄 Helpers/SystemSettings.cs
🎯 Chức năng:
   - Lấy các cấu hình hệ thống từ database (Settings table)
   - Cache để tránh query nhiều lần
   - Properties:
     • MaxLoginAttempts → Số lần đăng nhập sai tối đa
     • LockoutMinutes → Thời gian khóa tài khoản (phút)
     • ErrorUsernameExists → Mã lỗi username đã tồn tại
     • ErrorEmailExists → Mã lỗi email đã tồn tại

📄 Helpers/ValidationLimits.cs
🎯 Chức năng:
   - Lấy các giới hạn validation từ database metadata
   - Query INFORMATION_SCHEMA.COLUMNS để lấy độ dài tối đa/tối thiểu
   - Cache để tránh query nhiều lần
   - Properties:
     • MinUsernameLength / MaxUsernameLength
     • MinPasswordLength / MaxPasswordLength
     • MinEmailLength / MaxEmailLength
     • MinFullNameLength / MaxFullNameLength


================================================================================
8. STORED PROCEDURES ĐƯỢC SỬ DỤNG
================================================================================

╔════════════════════════════════════════════════════════════════════════════╗
║ USER MANAGEMENT                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

• sp_UserLogin          → frmDangNhap (Đăng nhập)
• sp_UserRegister       → frmDangKy (Đăng ký)
• sp_GetFirstUserId     → frmThemSuaTask, frmBaoCao (Lấy UserId)


╔════════════════════════════════════════════════════════════════════════════╗
║ TASK MANAGEMENT                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

• sp_GetTasksByFilter   → frmThemSuaTask (Lấy danh sách)
• sp_CreateTask         → frmThemSuaTask (Thêm mới)
• sp_UpdateTask         → frmThemSuaTask (Cập nhật)
• sp_DeleteTask         → frmThemSuaTask (Xóa - Soft Delete)
• sp_SearchTasks        → frmThemSuaTask (Tìm kiếm)


╔════════════════════════════════════════════════════════════════════════════╗
║ STATISTICS & REPORTING                                                     ║
╚════════════════════════════════════════════════════════════════════════════╝

• sp_GetDashboardStats  → frmBaoCao (Thống kê tổng quan)
• sp_GetUserStats       → frmBaoCao (Thống kê người dùng)
• sp_GetOverdueTasks    → frmBaoCao (Công việc quá hạn)
• sp_GetUsersWithOverdueTasks → frmBaoCao (Người dùng quá hạn)


╔════════════════════════════════════════════════════════════════════════════╗
║ HISTORY                                                                    ║
╚════════════════════════════════════════════════════════════════════════════╝

• sp_GetAllTaskHistory  → frmLichSu (Lấy toàn bộ lịch sử)
• sp_SearchTaskHistory  → frmLichSu (Tìm kiếm lịch sử)

================================================================================
9. CẤU TRÚC DỮ LIỆU
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ BẢNG: Users                                                             │
├─────────────────────────────────────────────────────────────────────────┤
│ • Id (int, PK)                                                          │
│ • Username (nvarchar, UNIQUE)                                           │
│ • PasswordHash (nvarchar)                                               │
│ • FullName (nvarchar)                                                   │
│ • Email (nvarchar)                                                      │
│ • CreatedDate (datetime)                                                │
│ • LastLoginDate (datetime, nullable)                                    │
│ • IsActive (bit)                                                        │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ BẢNG: Tasks                                                             │
├─────────────────────────────────────────────────────────────────────────┤
│ • Id (int, PK)                                                          │
│ • Title (nvarchar)                                                      │
│ • Description (nvarchar(max))                                           │
│ • UserId (int, FK → Users.Id)                                           │
│ • Priority (nvarchar) - High, Medium, Low                               │
│ • Status (nvarchar) - Todo, Doing, Done                                 │
│ • Category (nvarchar) - Work, Personal, Study                           │
│ • DueDate (datetime, nullable)                                          │
│ • CreatedDate (datetime)                                                │
│ • CompletedDate (datetime, nullable)                                    │
│ • IsDeleted (bit)                                                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ BẢNG: TaskHistory                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│ • Id (int, PK)                                                          │
│ • TaskId (int, FK → Tasks.Id)                                           │
│ • UserId (int, FK → Users.Id)                                           │
│ • Action (nvarchar) - Create, Update, Delete                            │
│ • OldValue (nvarchar(max), nullable)                                    │
│ • NewValue (nvarchar(max), nullable)                                    │
│ • ChangedDate (datetime)                                                │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
10. TÓM TẮT VÀ ĐÁNH GIÁ
================================================================================

✅ CÁC FORM ĐÃ HOÀN THÀNH:
────────────────────────────────────────────────────────────────────────────
1. Program.cs              → Entry point, khởi chạy frmDangNhap
2. frmDangNhap            → Đăng nhập với lockout mechanism
3. frmDangKy              → Đăng ký với validation đầy đủ
4. frmThemSuaTask         → Quản lý CRUD công việc (đã nhận userId)
5. frmBaoCao              → Thống kê và báo cáo (đã nhận userId)
6. frmLichSu              → Lịch sử thay đổi công việc

✅ CÁC HELPER CLASSES ĐÃ HOÀN THÀNH:
────────────────────────────────────────────────────────────────────────────
1. DatabaseHelper         → Data access layer
2. PasswordHelper         → Hash mật khẩu
3. CurrentUser            → Quản lý session user
4. SystemSettings         → Lấy cấu hình từ database
5. ValidationLimits       → Lấy giới hạn validation từ database

❌ CÁC FORM CHƯA CÓ CODE:
────────────────────────────────────────────────────────────────────────────
1. frmMain                → Form chính điều hướng (CHƯA TẠO)
                              - Đã có thiết kế chi tiết ở phần 5
                              - Cần implement theo mẫu code

⚠️ VẤN ĐỀ CẦN XỬ LÝ:
────────────────────────────────────────────────────────────────────────────
1. frmMain chưa được tạo → Sau khi đăng nhập, không có form nào mở
2. Form1 không sử dụng → Có thể xóa để tránh nhầm lẫn
3. frmDangNhap chưa mở frmMain → Cần sửa HandleSuccessfulLogin()

📝 GHI CHÚ:
────────────────────────────────────────────────────────────────────────────
- Tất cả các form trong project đã được liệt kê đầy đủ
- Các helper classes đã được tích hợp và sử dụng
- Constants đã được chuyển sang database (không hardcode)
- Stored procedures đã thay thế direct SQL queries
- UserId đã được truyền đúng cách qua constructor


================================================================================
KẾT THÚC TÀI LIỆU
================================================================================

📅 Cập nhật lần cuối: 2024
📝 Tác giả: Hệ thống Quản Lý Công Việc
📄 Phiên bản: 1.1

💡 LƯU Ý:
- Tài liệu này mô tả cấu trúc hiện tại của ứng dụng
- Các đề xuất cải thiện được đánh dấu ⚠️ và 💡
- Form chính (frmMain) đã được thiết kế chi tiết ở phần 5
- Cần triển khai frmMain theo thiết kế để hoàn thiện ứng dụng

📌 CÁC BƯỚC TRIỂN KHAI frmMain:
   1. Tạo file frmMain.cs và frmMain.Designer.cs
   2. Thiết kế giao diện theo sơ đồ ở phần 5
   3. Implement các method theo code mẫu
   4. Sửa frmDangNhap để mở frmMain sau khi đăng nhập
   5. Test luồng điều hướng đầy đủ

📋 DANH SÁCH FORM TRONG PROJECT:
   ✅ frmDangNhap.cs
   ✅ frmDangKy.cs
   ✅ frmThemSuaTask.cs
   ✅ frmBaoCao.cs
   ✅ frmLichSu.cs
   ❌ frmMain.cs (CHƯA CÓ - CẦN TẠO)
   ❌ Form1.cs (KHÔNG SỬ DỤNG - CÓ THỂ XÓA)

================================================================================
11. ĐỀ XUẤT CÁC FORM NÊN THÊM (TÙY CHỌN)
================================================================================

💡 Dựa trên phân tích ứng dụng hiện tại, đây là các form được đề xuất thêm
   để nâng cao trải nghiệm người dùng và tính đầy đủ của hệ thống:

⚠️ ĐÃ KIỂM TRA TRÙNG LẶP:
   - Đã loại bỏ frmTimKiemNangCao vì trùng chức năng với tìm kiếm trong 
     frmThemSuaTask (đã có btnTimKiem và sp_SearchTasks)

╔════════════════════════════════════════════════════════════════════════════╗
║ FORM ĐỀ XUẤT 1: frmQuanLyTaiKhoan (Quản Lý Tài Khoản)                    ║
║ Mức độ ưu tiên: ⭐⭐⭐⭐⭐ (RẤT QUAN TRỌNG)                                 ║
║ Độ phức tạp: ⭐⭐ (PHÙ HỢP SINH VIÊN NĂM 2)                               ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Tên file: frmQuanLyTaiKhoan.cs
🎯 Chức năng:
   - Xem thông tin tài khoản hiện tại
   - Đổi mật khẩu
   - Cập nhật thông tin cá nhân (Họ tên, Email)

🔑 Các thành phần đề xuất (Đơn giản):
   • GroupBox "Thông tin tài khoản"
     - lblUsername: Label hiển thị username (read-only) - lấy từ CurrentUser
     - txtHoTen: TextBox họ tên (có thể sửa)
     - txtEmail: TextBox email (có thể sửa)
     - lblNgayTao: Label ngày tạo tài khoản (hiển thị dd/MM/yyyy)
     - lblLanDangNhapCuoi: Label lần đăng nhập cuối (hiển thị dd/MM/yyyy)
   
   • GroupBox "Đổi mật khẩu"
     - txtMatKhauCu: TextBox mật khẩu cũ (PasswordChar = '*')
     - txtMatKhauMoi: TextBox mật khẩu mới (PasswordChar = '*')
     - txtXacNhanMatKhauMoi: TextBox xác nhận mật khẩu mới (PasswordChar = '*')
     - btnDoiMatKhau: Button đổi mật khẩu
   
   • btnCapNhat: Button "Cập nhật thông tin"
   • btnDong: Button "Đóng"

🔄 Luồng điều hướng (Đơn giản):
   - Được gọi từ frmMain: new frmQuanLyTaiKhoan().ShowDialog()
   - Constructor: Lấy userId từ CurrentUser.GetUserId()
   - Load: Gọi sp_GetUserById để lấy thông tin user
   - btnCapNhat: Validate → Gọi sp_UpdateUser (đã có) → Thông báo thành công
   - btnDoiMatKhau: Validate → Hash mật khẩu mới → Gọi sp_ChangePassword (đã có)

💾 Database Operations (Đã có sẵn):
   ✅ sp_GetUserById → Đã có trong AdditionalProcedures.sql
   ✅ sp_UpdateUser → Đã có trong AdditionalProcedures.sql  
   ✅ sp_ChangePassword → Đã có trong AdditionalProcedures.sql
   ✅ PasswordHelper.HashPassword() → Đã có helper class

🛡️ Bảo mật (Đơn giản):
   - Kiểm tra mật khẩu cũ trước khi đổi (dùng PasswordHelper.HashPassword so sánh)
   - Hash mật khẩu mới bằng PasswordHelper.HashPassword() (đã có)
   - Validate email dùng ValidationLimits (đã có helper)
   - Validate độ dài họ tên dùng ValidationLimits (đã có helper)

📝 ĐÁNH GIÁ PHÙ HỢP:
   ✅ Code đơn giản: Chỉ cần gọi SP và hiển thị dữ liệu
   ✅ Tận dụng helper classes đã có (PasswordHelper, ValidationLimits)
   ✅ Pattern giống frmDangKy, frmDangNhap
   ✅ Không cần kiến thức nâng cao

──────────────────────────────────────────────────────────────────────────────

⚠️ LƯU Ý: Form "frmTimKiemNangCao" đã được loại bỏ khỏi danh sách đề xuất vì TRÙNG
   chức năng với frmThemSuaTask (đã có tìm kiếm công việc qua btnTimKiem và 
   sp_SearchTasks). Mặc dù form đề xuất có nhiều filter hơn, nhưng mục đích chính
   vẫn là tìm kiếm công việc, nên không cần thiết tạo form riêng.

──────────────────────────────────────────────────────────────────────────────

📊 TÓM TẮT ĐỀ XUẤT:

┌──────────────────────────────┬──────────────┬──────────────┬──────────────────┐
│ Form đề xuất                 │ Ưu tiên      │ Độ phức tạp  │ Khuyến nghị      │
├──────────────────────────────┼──────────────┼──────────────┼──────────────────┤
│ frmQuanLyTaiKhoan            │ ⭐⭐⭐⭐⭐   │ ⭐⭐ (Đơn)   │ ✅ NÊN TẠO        │
│ frmAdminDashboard (MỨC 1)    │ ⭐⭐⭐⭐     │ ⭐⭐ (Đơn)   │ ✅ NÊN CÓ        │
└──────────────────────────────┴──────────────┴──────────────┴──────────────────┘

❌ ĐÃ XÓA: 
   - frmTimKiemNangCao (TRÙNG với chức năng tìm kiếm trong frmThemSuaTask)
   - frmChiTietTask (Bỏ theo yêu cầu)
   - frmHuongDan (Bỏ theo yêu cầu)

💡 ĐÁNH GIÁ VÀ KHUYẾN NGHỊ CHO SINH VIÊN NĂM 2:

   ✅ ƯU TIÊN 1: frmMain (BẮT BUỘC)
      - Form chính điều hướng, đã có thiết kế chi tiết
      - Độ phức tạp: ⭐⭐
   
   ✅ ƯU TIÊN 2: frmQuanLyTaiKhoan (NÊN LÀM)
      - Quan trọng: Người dùng cần đổi mật khẩu
      - Đơn giản: Chỉ gọi SP đã có sẵn
      - Độ phức tạp: ⭐⭐ (Tương đương frmDangKy)
   
   ✅ ƯU TIÊN 3: frmAdminDashboard MỨC 1 (NÊN LÀM NẾU CÓ ADMIN)
      - Tab 1: Tổng quan (giống frmBaoCao)
      - Tab 2: Quản lý users (đơn giản: UPDATE IsActive, DELETE)
      - Độ phức tạp: ⭐⭐
   
   ❌ KHÔNG KHUYẾN NGHỊ: frmAdminDashboard Tab "Log đăng nhập"
      - Quá phức tạp: Cần tạo bảng mới, trigger, xử lý log
      - Không cần thiết cho đồ án năm 2
      - Độ phức tạp: ⭐⭐⭐⭐
   
   📝 KẾT LUẬN:
      Với sinh viên năm 2, nên tập trung vào:
      1. frmMain (bắt buộc)
      2. frmQuanLyTaiKhoan (nên có)
      3. frmAdminDashboard MỨC 1 (nếu cần admin)
      
      Có thể bỏ qua: Tab Log đăng nhập

──────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════╗
║ FORM ĐỀ XUẤT 5: frmAdminDashboard (Admin Dashboard - MỨC 1)               ║
║ Mức độ ưu tiên: ⭐⭐⭐⭐ (CHO ADMIN/QUẢN TRỊ VIÊN)                          ║
║ Độ phức tạp: ⭐⭐ (ĐƠN GIẢN - PHÙ HỢP SINH VIÊN NĂM 2)                    ║
╚════════════════════════════════════════════════════════════════════════════╝

📄 Tên file: frmAdminDashboard.cs
🎯 Chức năng (MỨC 1: Admin Cơ Bản - Đơn giản):

   ✅ MỨC 1: Admin Cơ Bản (Chỉ làm các tính năng này):
   - Tab 1: Thống kê tổng quan hệ thống
     • Tổng số users (active/inactive)
     • Tổng số tasks
     • Tasks theo status (Todo, Doing, Done)
     • Tasks quá hạn
   
   - Tab 2: Quản lý Users
     • Xem danh sách tất cả users
     • Khóa/Mở khóa tài khoản (Toggle IsActive)
     • Xóa user (có xác nhận)
     • Tìm kiếm user
   
   → Độ phức tạp: ⭐⭐ (Tương đương frmBaoCao, frmThemSuaTask)
   → Thời gian: 2-3 ngày

🔑 Phương pháp "CÁCH 2" - Nhận diện Admin:
   CÁCH 1 (Nếu có Role field): Kiểm tra Role = 'Admin'
   CÁCH 2 (Không có Role field): Kiểm tra Username = 'admin' (hardcoded)
   
   → Hiện tại database không có Role field, nên dùng CÁCH 2:
      if (CurrentUser.GetUsername().ToLower() == "admin")
      {
          // Hiển thị menu Admin
          // Mở frmAdminDashboard
      }

🔑 Các thành phần đề xuất (MỨC 1: Admin Cơ Bản - Chỉ 2 Tab):
   • TabControl với 2 Tab đơn giản:
   
     ┌────────────────────────────────────────────────────────────────────┐
     │ Tab 1: "Tổng quan" (MỨC 1)                                        │
     ├────────────────────────────────────────────────────────────────────┤
     │ GroupBox "Thống kê hệ thống":                                     │
     │   • lblTongUsers: Label "Tổng số người dùng: [số]"                 │
     │   • lblUsersActive: Label "Người dùng đang hoạt động: [số]"        │
     │   • lblUsersInactive: Label "Người dùng bị khóa: [số]"             │
     │   • lblTongTasks: Label "Tổng số công việc: [số]"                  │
     │                                                                     │
     │ GroupBox "Thống kê theo trạng thái":                               │
     │   • lblTasksTodo: Label "Chưa bắt đầu: [số]"                       │
     │   • lblTasksDoing: Label "Đang làm: [số]"                          │
     │   • lblTasksDone: Label "Hoàn thành: [số]"                         │
     │   • lblTasksOverdue: Label "Quá hạn: [số]"                         │
     └────────────────────────────────────────────────────────────────────┘
     
     ┌────────────────────────────────────────────────────────────────────┐
     │ Tab 2: "Quản lý người dùng" (MỨC 1)                               │
     ├────────────────────────────────────────────────────────────────────┤
     │ dgvUsers: DataGridView danh sách tất cả users                      │
     │   Các cột: Id, Username, FullName, Email, CreatedDate,             │
     │            LastLoginDate, IsActive, Tổng Tasks                     │
     │                                                                     │
     │ GroupBox "Thao tác":                                               │
     │   • btnKhoaUser: Button "Khóa/Mở khóa" (Toggle IsActive)          │
     │   • btnXoaUser: Button "Xóa user" (với xác nhận)                  │
     │   • txtTimKiemUser: TextBox tìm kiếm user                          │
     │   • btnTimKiemUser: Button "Tìm kiếm"                              │
     │   • btnLamMoi: Button "Làm mới" (reload tất cả users)             │
     └────────────────────────────────────────────────────────────────────┘

🔄 Luồng điều hướng (MỨC 1: Admin Cơ Bản - Đơn giản):
   
   ┌──────────────────────────────────────────────────────────────┐
   │ 1. Kiểm tra quyền Admin:                                    │
   │    - Trong frmDangNhap hoặc frmMain                        │
   │    - if (CurrentUser.GetUsername().ToLower() == "admin")    │
   │      {                                                       │
   │          // Hiển thị button "Admin Panel"                    │
   │          // Mở frmAdminDashboard                              │
   │      }                                                       │
   │                                                              │
   │ 2. Khi mở frmAdminDashboard:                                │
   │    ├─ Tab 1 "Tổng quan":                                    │
   │    │   └─ Gọi sp_GetSystemStatistics → Hiển thị tổng quan   │
   │    │                                                         │
   │    └─ Tab 2 "Quản lý người dùng":                          │
   │        └─ Gọi sp_GetAllUsers → Load danh sách users         │
   │                                                              │
   │ 3. Tab "Quản lý người dùng" (MỨC 1):                       │
   │    - btnKhoaUser → Toggle IsActive (Khóa/Mở khóa)          │
   │    - btnXoaUser → Xóa user (có xác nhận, cascade delete)   │
   │        └─ btnLamMoi_Click:                                  │
   │            └─ Reload tất cả users (gọi lại sp_GetAllUsers)  │
   └──────────────────────────────────────────────────────────────┘

💾 Database Operations (MỨC 1: Chỉ cần các SP cơ bản):

   ✅ CẦN TẠO MỚI (theo pattern hiện tại - đơn giản):
   • sp_GetAllUsers → Lấy tất cả users với thống kê
     - SELECT u.*, COUNT(t.Id) AS TongTasks
       FROM Users u LEFT JOIN Tasks t ON u.Id = t.UserId AND t.IsDeleted = 0
       GROUP BY u.Id, u.Username, ...
     - Dùng cho Tab 2
   
   • sp_GetSystemStatistics → Thống kê tổng quan hệ thống
     - Tổng users, users active/inactive
     - Tổng tasks, tasks theo status (Todo, Doing, Done)
     - Tổng tasks quá hạn
     - Dùng cho Tab 1
   
   • sp_UpdateUserStatus → Khóa/Mở khóa user (Toggle IsActive)
     - UPDATE Users SET IsActive = @IsActive WHERE Id = @UserId
     - Dùng cho Tab 2
   
   • sp_DeleteUser → Xóa user (cascade delete)
     - DELETE FROM Users WHERE Id = @UserId
     - CASCADE sẽ tự xóa Tasks và TaskHistory (FK constraint)
     - Dùng cho Tab 2 - btnXoaUser
     - Có thể thêm kiểm tra: Không cho phép xóa admin
   
   ✅ KHÔNG CẦN (MỨC 1):
   • sp_GetAllTasksAdmin → Không cần (Tab 3 đã bỏ)
   • sp_GetTopUsers → Không cần (Tab 4 đã bỏ)
   • sp_GetLoginLog → Không cần (Tab 5 đã bỏ)
   • Bảng LoginLog → Không cần

🛡️ Bảo mật (MỨC 1 - Đơn giản):
   - Chỉ user có username = "admin" mới được truy cập
   - Validate quyền: Kiểm tra IsAdmin() trước khi mở form
   - Xác nhận trước khi khóa/xóa user (MessageBox.YesNo)
   - Không cho phép xóa user admin (kiểm tra Username != "admin")

📋 CẬP NHẬT CẦN THIẾT (MỨC 1 - Đơn giản):
   
   ✅ Database (Chỉ cần 4 SP cơ bản):
   1. sp_GetAllUsers → Lấy danh sách users với thống kê
   2. sp_GetSystemStatistics → Thống kê tổng quan hệ thống
   3. sp_UpdateUserStatus → Khóa/Mở khóa user
   4. sp_DeleteUser → Xóa user
   
   ✅ Code (theo pattern hiện tại - rất đơn giản):
   - frmMain: Thêm button "Admin Panel"
     - Kiểm tra: btnAdminPanel.Visible = (CurrentUser.GetUsername().ToLower() == "admin")
     - Click: new frmAdminDashboard().ShowDialog()
   
   - frmAdminDashboard: Chỉ 2 Tab
     - Tab 1 "Tổng quan": 
       • Gọi sp_GetSystemStatistics
       • Hiển thị lên Labels (giống frmBaoCao)
       • btnLamMoi → Reload
     
     - Tab 2 "Quản lý người dùng":
       • Gọi sp_GetAllUsers
       • Hiển thị trong DataGridView
       • Format ngày: "dd/MM/yyyy"
       • Format IsActive: "Hoạt động" / "Bị khóa"
       • btnKhoaUser → Toggle IsActive
       • btnXoaUser → Xóa (có xác nhận)
       • btnTimKiemUser → Filter trong DataTable
   
   - Helper: Có thể tạo AdminHelper (tùy chọn)
     - public static bool IsAdmin() 
       { return CurrentUser.GetUsername().ToLower() == "admin"; }
   
   ✅ UI/UX (Đơn giản):
   - Chỉ dùng Labels và DataGridView (không cần Chart)
   - Format dữ liệu giống các form khác
   - Xác nhận trước khi khóa/xóa (MessageBox)
   
   ⚠️ KHÔNG CẦN:
   - Tab 3, 4, 5 (đã bỏ)
   - Bảng LoginLog
   - Chart controls
   - Export Excel/PDF

📊 VÍ DỤ CODE IMPLEMENTATION (theo pattern hiện tại):

   ┌──────────────────────────────────────────────────────────────┐
   │ Tab 1 - Load thống kê tổng quan:                            │
   ├──────────────────────────────────────────────────────────────┤
   │ private void TaiThongKeTongQuan()                           │
   │ {                                                           │
   │     try                                                     │
   │     {                                                       │
   │         DataTable bangDuLieu = DatabaseHelper               │
   │             .ExecuteStoredProcedure("sp_GetSystemStatistics");│
   │                                                             │
   │         if (bangDuLieu.Rows.Count > 0)                      │
   │         {                                                   │
   │             DataRow dong = bangDuLieu.Rows[0];              │
   │             lblTongUsers.Text = "Tổng số người dùng: " +    │
   │                 dong["TongUsers"].ToString();               │
   │             lblUsersActive.Text = "Người dùng đang hoạt động: "│
   │                 + dong["UsersActive"].ToString();           │
   │             // ... các label khác                           │
   │         }                                                   │
   │     }                                                       │
   │     catch (Exception loi)                                   │
   │     {                                                       │
   │         MessageBox.Show("Lỗi: " + loi.Message);            │
   │     }                                                       │
   │ }                                                           │
   └──────────────────────────────────────────────────────────────┘
   
   📋 LƯU Ý:
   - Sử dụng stored procedures thay vì direct SQL (bảo mật hơn)
   - Format dữ liệu giống như các form khác (dd/MM/yyyy, %, ...)
   - Sử dụng DatabaseHelper.ExecuteStoredProcedure giống frmBaoCao

================================================================================

💡 LƯU Ý VỀ PHIÊN BẢN NÂNG CẤP:
   Hiện tại chỉ triển khai frmAdminDashboard ở MỨC 1 (2 Tab cơ bản).
   Nếu muốn nâng cấp cho sinh viên năm 3, có thể thêm:
   - Tab Log đăng nhập (Audit Trail)
   - Tab Export Excel/PDF
   - Tab Biểu đồ trực quan
   - Tab Cấu hình hệ thống nâng cao
   - Tab Phân quyền RBAC (nếu thêm Role field)

──────────────────────────────────────────────────────────────────────────────

